import { MarkedDatesType } from '../types/dateData'

export const stylesContainer: MarkedDatesType[] = [
  {
    customStyles: {
      container: {
        backgroundColor: "#ef4444", // Amarillo dorado (personaliza el color)
        borderRadius: 20, // Hace que sea un círculo
        justifyContent: "center",
        alignItems: "center",
      },
      text: {
        color: "#fff", // Texto negro para contraste
        fontWeight: "bold",
      },
    }
  }
]